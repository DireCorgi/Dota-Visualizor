const getButtons = () => {
  const dataButton = document.getElementById('get-data');
  const advButton = document.getElementById('graph-advantage');
  return { dataButton, advButton };
};

export default getButtons;
